name: Deploy to Vercel

on:
  push:
    branches: ["master"] # or any branch you want to deploy from

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18.x # Specify your Node.js version here

      - name: Install dependencies
        run: npm ci

      - name: Build the project
        run: npm run build # Assuming you have a build script for production

      - name: Deploy to Vercel
        run: npx vercel --prod --token chfsJS8i04OWfVUCD428Nkmw
        env:
          VERCEL_ORG_ID: zggJxo5v2Lx7d16oWJwPUqgE
          VERCEL_PROJECT_ID: prj_oCVhJypkkdjZZBiV7YTEPslbNDg1
